- [ ] Investigate `Optimizer.purs` to see if/how type annotations on AST nodes are preserved during inlining.
- [ ] Fix the inliner to either substitute type variables appropriately or ensure `Monomorphize.purs` can reach and strip `ForAll` bindings from inlined foreign module nodes.
- [ ] Re-run benchmarks (`Prime Sieve`) to confirm `go__go` loops inside inlined functions (e.g., `reverse`) compile to strict Go structs rather than falling back to `gopurs_runtime.Value` interfaces.
